diff --git a/src/pages/NewReviewPage.tsx b/src/pages/NewReviewPage.tsx
index 42d6d5d..d70272e 100644
--- a/src/pages/NewReviewPage.tsx
+++ b/src/pages/NewReviewPage.tsx
@@ -1,4 +1,4 @@
-import { FiUpload, FiCode, FiAlertCircle } from "react-icons/fi";
+import { FiCode, FiAlertCircle } from "react-icons/fi";
 
 export default function NewReviewPage() {
   return (
diff --git a/vite.config.ts b/vite.config.ts
index dcd5bb9..6dd33c4 100644
--- a/vite.config.ts
+++ b/vite.config.ts
@@ -6,7 +6,7 @@ import svgr from 'vite-plugin-svgr'
 // Custom plugin to handle ?import&react syntax (alias to ?react)
 const svgImportPlugin = () => ({
   name: 'svg-import-alias',
-  resolveId(id) {
+  resolveId(id: string) {
     // Transform ?import&react to ?react for vite-plugin-svgr
     if (id.includes('?import&react')) {
       return id.replace('?import&react', '?react');
@@ -16,7 +16,7 @@ const svgImportPlugin = () => ({
 });
 
 // https://vite.dev/config/
-export default defineConfig(({ command }) => ({
+export default defineConfig(({ command: _command }) => ({
   plugins: [
     react(),
     tailwindcss(),
